{
  "name": "cv",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "dev": "bun ./index.html",
    "build": "tsc --noEmit && bun scripts/build.ts",
    "check": "tsc --noEmit",
    "fonts:update": "bun scripts/update-fonts.ts"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.8",
    "hyphen": "^1.14.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
