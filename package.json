{
  "name": "cv",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "dev": "bun ./index.html",
    "build": "bun scripts/build.ts",
    "preview": "bun run build && bun scripts/preview.ts",
    "check": "tsgo --noEmit",
    "test": "bun test",
    "fonts:update": "bun scripts/update-fonts.ts",
    "og:update": "bun scripts/og-image.ts",
    "deploy": "bun scripts/deploy.ts"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.8",
    "hyphen": "^1.14.1"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "@typescript/native-preview": "^7.0.0-dev.20260707.2"
  }
}
