# Plan A — Blog SSG (contenu, URLs, pages, build)

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformer le site CV en blog personnel multilingue dont le CV
devient une page, en générant tout le `dist/` avec les fonctions natives de Bun.

**Architecture:** Le contenu vit en Markdown sur le disque
(`content/posts/<slug>/<lang>.md`), découvert par `Bun.Glob`, rendu par
`Bun.markdown.html()` puis réécrit par `HTMLRewriter`. Le rendu reste des
fonctions pures produisant des chaînes, partagées par le build et le client. La
disposition met l'anglais à la racine et chaque autre langue dans son dossier,
avec des chemins d'assets relatifs calculés depuis la profondeur de la page.

**Tech Stack:** Bun 1.3.14 (`Bun.build`, `Bun.markdown`, `Bun.Glob`,
`HTMLRewriter`, `Bun.$`, `bun:test`), TypeScript sans framework, `tsgo` pour le
typecheck, `deno fmt` + `deno lint` pour le formatage.

**Spec:**
[../specs/2026-08-08-blog-ssg-design.md](../specs/2026-08-08-blog-ssg-design.md)

## Périmètre

Ce plan est le premier de trois. Il livre un blog multilingue complet dans
`dist/`. Ne sont **pas** ici :

- **Plan B — Justification** : `src/richtext.ts`, refonte de la signature de
  `linebreak.ts`, ordonnancement par `IntersectionObserver`, vérifications
  navigateur (copie, recherche, CLS). Les articles produits par ce plan-ci
  s'affichent avec le `text-align: justify` natif en attendant.
- **Plan C — Déploiement OSS** : OIDC, STS, upload présigné, purge CDN,
  workflow. Indépendant : il peut démarrer en parallèle, et il porte le pic de
  risque du projet (compatibilité SigV4 d'OSS).

## Global Constraints

Ces règles valent pour **chaque** tâche ; elles ne sont pas répétées ensuite.

- **Aucune dépendance nouvelle.** Le repo en compte deux (`@chenglou/pretext`,
  `hyphen`). Tout ce qui est ajouté ici s'écrit avec Bun ou la bibliothèque
  standard.
- **`src/render*` reste pur** : pas d'accès au DOM, pas de lecture disque. Ces
  fonctions tournent sous Bun pendant le build.
- **Progressive enhancement stricte.** Le HTML pré-rendu est le produit. Sans
  JS, le blog est complet, lisible et navigable.
- **Fin de développement** : `deno fmt` **puis** `deno lint` à la racine.
  `deno
  fmt` fait autorité, y compris sur les `.md` ; ne jamais annuler son
  reformatage.
- **Gates** : `bun run check` (tsgo) et `bun test` doivent passer avant chaque
  commit.
- **Sept langues**, verrouillées : `en`, `fr`, `pt` (européen, orthographe
  pré-1990), `es` (péninsulaire), `zh` (Hans), `zh-hant` (Taïwan), `zh-hk`
  (projection lexicale de `zh-hant`, absente du sélecteur).
- **Après toute modification de texte visible** : `bun run fonts:update`, sinon
  un glyphe absent du sous-ensemble s'affiche en tofu.
- **Commits en français**, style conventionnel (`feat:`, `fix:`, `refactor:`,
  `test:`, `docs:`), comme l'historique du repo.

## Structure des fichiers

**Créés**

| Fichier                       | Responsabilité                                                         |
| ----------------------------- | ---------------------------------------------------------------------- |
| `scripts/frontmatter.ts`      | Découper et valider le frontmatter. Pur, sans disque.                  |
| `scripts/frontmatter.test.ts` | Sa grammaire et tous ses refus.                                        |
| `scripts/markdown.ts`         | `Bun.markdown.html()` + passe `HTMLRewriter` + garde-fou HTML.         |
| `scripts/markdown.test.ts`    | Chaque réécriture, et le rejet du HTML dangereux.                      |
| `scripts/content.ts`          | Découverte disque (`Bun.Glob`), brouillons, collisions, projection HK. |
| `scripts/content.test.ts`     | Composition du corpus depuis une arborescence temporaire.              |
| `scripts/relay.ts`            | Pages-relais des anciennes URLs.                                       |
| `scripts/relay.test.ts`       | Canonical, `noindex`, cible correcte.                                  |
| `scripts/preview.ts`          | Serveur statique sur `dist/`.                                          |
| `src/urls.ts`                 | `rel(depth)`, chemins et liens entre pages, slugs réservés.            |
| `src/urls.test.ts`            | Profondeurs, liens croisés, collisions.                                |
| `src/post.ts`                 | Type `PostMeta` + sélection pure (par langue, tri, traductions).       |
| `src/post.test.ts`            | Index par langue, tri, langues disponibles.                            |
| `src/render/shell.ts`         | Nav, sélecteur de langue, pied de page, `section()`.                   |
| `src/render/cv.ts`            | Les neuf sections actuelles, déplacées sans modification.              |
| `src/render/blog.ts`          | Accueil, index blog, page d'article.                                   |
| `src/render/blog.test.ts`     | Markup des trois types de page, repli du sélecteur.                    |
| `content/posts/**`            | Le corpus de test : 10 articles complets + 2 partiels.                 |

**Modifiés**

| Fichier               | Changement                                               |
| --------------------- | -------------------------------------------------------- |
| `src/render.ts`       | Devient une façade ré-exportant les trois modules.       |
| `src/translations.ts` | Bloc `blog` en 6 langues + `toHongKongText` exporté.     |
| `scripts/build.ts`    | Émet le nouveau jeu de pages, chemins d'assets relatifs. |
| `scripts/feed.ts`     | Les items deviennent les articles.                       |
| `scripts/sitemap.ts`  | URLs d'articles, `lastmod` par fichier source.           |
| `scripts/glyphs.ts`   | Scanne `content/**/*.md`.                                |
| `package.json`        | Script `preview`.                                        |

---

### Task 1: Parseur de frontmatter

**Files:**

- Create: `scripts/frontmatter.ts`
- Test: `scripts/frontmatter.test.ts`

**Interfaces:**

- Consumes: rien.
- Produces:
  ```ts
  export interface Frontmatter {
    title: string;
    date: string; // YYYY-MM-DD
    summary?: string;
    tags: readonly string[];
    draft: boolean;
    updated?: string; // YYYY-MM-DD
  }
  export function parseFrontmatter(
    source: string,
    path: string,
  ): { data: Frontmatter; body: string };
  ```

- [x] **Step 1: Écrire les tests qui échouent**

```ts
/*
 * Le frontmatter est un sous-ensemble volontairement minuscule : pas de YAML,
 * donc pas de dépendance, et une grammaire assez petite pour que chaque refus
 * soit un message précis plutôt qu'un comportement surprenant.
 */

import { expect, test } from "bun:test";
import { parseFrontmatter } from "./frontmatter.ts";

const PATH = "content/posts/x/fr.md";
const block = (...lines: string[]) =>
  ["---", ...lines, "---", "", "Le corps."].join("\n");

test("lit les champs requis et rend le corps sans le bloc", () => {
  const { data, body } = parseFrontmatter(
    block("title: Mesurer le texte", "date: 2026-08-08"),
    PATH,
  );

  expect(data.title).toBe("Mesurer le texte");
  expect(data.date).toBe("2026-08-08");
  expect(data.tags).toEqual([]);
  expect(data.draft).toBe(false);
  expect(body).toBe("Le corps.");
});

test("lit les champs optionnels", () => {
  const { data } = parseFrontmatter(
    block(
      "title: T",
      "date: 2026-08-08",
      "summary: Un résumé.",
      "tags: [typographie, bun]",
      "draft: true",
      "updated: 2026-08-09",
    ),
    PATH,
  );

  expect(data.summary).toBe("Un résumé.");
  expect(data.tags).toEqual(["typographie", "bun"]);
  expect(data.draft).toBe(true);
  expect(data.updated).toBe("2026-08-09");
});

test("retire une paire de guillemets englobants, pas ceux du texte", () => {
  const { data } = parseFrontmatter(
    block(`title: "Le mot « juste »"`, "date: 2026-08-08"),
    PATH,
  );

  expect(data.title).toBe("Le mot « juste »");
});

test("un deux-points dans la valeur n'est pas un séparateur", () => {
  const { data } = parseFrontmatter(
    block("title: Bun : ce qu'il sait faire", "date: 2026-08-08"),
    PATH,
  );

  expect(data.title).toBe("Bun : ce qu'il sait faire");
});

test.each([
  ["sans bloc du tout", "Juste du texte."],
  ["bloc non fermé", "---\ntitle: T\ndate: 2026-08-08"],
  ["title manquant", block("date: 2026-08-08")],
  ["date manquante", block("title: T")],
  ["date malformée", block("title: T", "date: 8 août 2026")],
  ["date impossible", block("title: T", "date: 2026-02-31")],
  ["clé inconnue", block("title: T", "date: 2026-08-08", "auteur: X")],
  ["clé dupliquée", block("title: T", "title: U", "date: 2026-08-08")],
  ["draft non booléen", block("title: T", "date: 2026-08-08", "draft: oui")],
  ["tags hors crochets", block("title: T", "date: 2026-08-08", "tags: a, b")],
  ["ligne sans deux-points", block("title: T", "date: 2026-08-08", "bonjour")],
])("refuse : %s", (_label, source) => {
  expect(() => parseFrontmatter(source, PATH)).toThrow(PATH);
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test scripts/frontmatter.test.ts` Expected: FAIL —
`Cannot find module './frontmatter.ts'`

- [x] **Step 3: Implémenter**

```ts
/*
 * Frontmatter, sous-ensemble minimal et strict.
 *
 * Pas de YAML : la grammaire tient en `clé: valeur`, plus `tags: [a, b]`. Tout
 * ce qui sort de là est refusé avec le chemin du fichier, parce qu'un
 * frontmatter mal lu produit une page silencieusement fausse — un titre vide,
 * une date de 1970 — là où une erreur de build se corrige en dix secondes.
 */

const FENCE = "---";
const DATE = /^\d{4}-\d{2}-\d{2}$/;
const KNOWN = ["title", "date", "summary", "tags", "draft", "updated"] as const;
type Key = (typeof KNOWN)[number];

export interface Frontmatter {
  title: string;
  date: string;
  summary?: string;
  tags: readonly string[];
  draft: boolean;
  updated?: string;
}

function fail(path: string, message: string): never {
  throw new Error(`${path}: ${message}`);
}

/** Une date réelle, pas seulement une chaîne bien formée : 2026-02-31 n'existe pas. */
function isRealDate(value: string): boolean {
  if (!DATE.test(value)) return false;
  const date = new Date(`${value}T00:00:00Z`);
  return !Number.isNaN(date.getTime()) &&
    date.toISOString().slice(0, 10) === value;
}

/** Une seule paire de guillemets englobants est du balisage, pas du texte. */
function unquote(value: string): string {
  const quoted = (q: string) =>
    value.length >= 2 && value.startsWith(q) && value.endsWith(q);
  return quoted('"') || quoted("'") ? value.slice(1, -1) : value;
}

export function parseFrontmatter(
  source: string,
  path: string,
): { data: Frontmatter; body: string } {
  const lines = source.split("\n");
  if (lines[0]?.trim() !== FENCE) fail(path, "aucun bloc frontmatter");

  const end = lines.findIndex((line, i) => i > 0 && line.trim() === FENCE);
  if (end === -1) fail(path, "bloc frontmatter non fermé");

  const raw = new Map<Key, string>();
  for (const line of lines.slice(1, end)) {
    if (!line.trim()) continue;
    const colon = line.indexOf(":");
    if (colon === -1) fail(path, `ligne sans « : » : ${JSON.stringify(line)}`);
    const key = line.slice(0, colon).trim();
    if (!(KNOWN as readonly string[]).includes(key)) {
      fail(path, `clé inconnue « ${key} » (attendu : ${KNOWN.join(", ")})`);
    }
    if (raw.has(key as Key)) fail(path, `clé dupliquée « ${key} »`);
    raw.set(key as Key, unquote(line.slice(colon + 1).trim()));
  }

  const title = raw.get("title");
  if (!title) fail(path, "« title » est requis");
  const date = raw.get("date");
  if (!date) fail(path, "« date » est requise");
  if (!isRealDate(date)) fail(path, `date invalide « ${date} » (YYYY-MM-DD)`);

  const updated = raw.get("updated");
  if (updated !== undefined && !isRealDate(updated)) {
    fail(path, `updated invalide « ${updated} » (YYYY-MM-DD)`);
  }

  const rawDraft = raw.get("draft");
  if (rawDraft !== undefined && rawDraft !== "true" && rawDraft !== "false") {
    fail(path, `draft doit valoir true ou false, pas « ${rawDraft} »`);
  }

  const rawTags = raw.get("tags");
  if (rawTags !== undefined && !/^\[.*\]$/.test(rawTags)) {
    fail(path, `tags doit être de la forme [a, b], pas « ${rawTags} »`);
  }
  const tags = rawTags
    ? rawTags.slice(1, -1).split(",").map((t) => t.trim()).filter(Boolean)
    : [];

  const summary = raw.get("summary");
  return {
    data: {
      title,
      date,
      tags,
      draft: rawDraft === "true",
      ...(summary ? { summary } : {}),
      ...(updated ? { updated } : {}),
    },
    body: lines.slice(end + 1).join("\n").trim(),
  };
}
```

- [x] **Step 4: Lancer les tests et vérifier qu'ils passent**

Run: `bun test scripts/frontmatter.test.ts && bun run check` Expected: PASS,
aucune erreur de type.

- [x] **Step 5: Commit**

```bash
deno fmt scripts/frontmatter.ts scripts/frontmatter.test.ts && deno lint
git add scripts/frontmatter.ts scripts/frontmatter.test.ts
git commit -m "feat: parser de frontmatter strict, sans dépendance YAML"
```

---

### Task 2: Pipeline Markdown

**Files:**

- Create: `scripts/markdown.ts`
- Test: `scripts/markdown.test.ts`

**Interfaces:**

- Consumes: `Lang` de `src/translations.ts`.
- Produces:
  ```ts
  export function assertSafeMarkdown(source: string, path: string): void;
  export function slugifyHeading(text: string): string;
  export async function renderMarkdown(
    body: string,
    lang: Lang,
  ): Promise<string>;
  ```

**Contexte mesuré** (ne pas re-vérifier) : `Bun.markdown.html()` rend du GFM,
**ne retire pas** le frontmatter, et **laisse passer le HTML brut**.

- [x] **Step 1: Écrire les tests qui échouent**

```ts
/*
 * Le rendu Markdown est la seule porte par laquelle du HTML non écrit à la main
 * entre dans les pages. Ces tests tiennent les deux bouts : les réécritures que
 * la passe HTMLRewriter doit faire, et ce que le garde-fou doit refuser.
 */

import { expect, test } from "bun:test";
import {
  assertSafeMarkdown,
  renderMarkdown,
  slugifyHeading,
} from "./markdown.ts";

test("les titres reçoivent un id slugifié", async () => {
  const html = await renderMarkdown("## Mesurer le texte, sans reflow", "fr");
  expect(html).toContain('<h2 id="mesurer-le-texte-sans-reflow"');
});

test("un id de titre reste unique quand le texte se répète", async () => {
  const html = await renderMarkdown("## Notes\n\ntexte\n\n## Notes", "fr");
  expect(html).toContain('id="notes"');
  expect(html).toContain('id="notes-2"');
});

test("les liens externes reçoivent rel, les internes non", async () => {
  const html = await renderMarkdown(
    "[dehors](https://exemple.test) et [dedans](./cv.html)",
    "fr",
  );
  expect(html).toContain('rel="noopener noreferrer"');
  expect(html.match(/rel="noopener noreferrer"/g)).toHaveLength(1);
});

test("une table est enveloppée dans un conteneur défilable", async () => {
  const html = await renderMarkdown("| a | b |\n|---|---|\n| 1 | 2 |", "fr");
  expect(html).toContain('<div class="table-scroll">');
  expect(html.indexOf('<div class="table-scroll">'))
    .toBeLessThan(html.indexOf("<table>"));
});

test("les images sont paresseuses", async () => {
  const html = await renderMarkdown("![chat](a.png)", "fr");
  expect(html).toContain('loading="lazy"');
  expect(html).toContain('decoding="async"');
});

test("un run CJK dans une page latine est marqué (RGAA 8.7)", async () => {
  const html = await renderMarkdown("On dit 微辣 à Chengdu.", "fr");
  expect(html).toContain('<span lang="zh-Hans">微辣</span>');
});

test("une page chinoise ne marque rien : il n'y a pas de changement de langue", async () => {
  const html = await renderMarkdown("成都的微辣。", "zh");
  expect(html).not.toContain('lang="zh-Hans"');
});

test.each([
  ["<script>alert(1)</script>"],
  ['<iframe src="x"></iframe>'],
  ['<img src=x onerror="alert(1)">'],
  ["<SCRIPT>alert(1)</SCRIPT>"],
])("le garde-fou refuse %s", (source) => {
  expect(() => assertSafeMarkdown(source, "content/posts/x/fr.md"))
    .toThrow("content/posts/x/fr.md");
});

test("le garde-fou laisse passer du Markdown ordinaire", () => {
  expect(() =>
    assertSafeMarkdown("Du *texte* et `du code`.", "content/posts/x/fr.md")
  ).not.toThrow();
});

test("slugifyHeading translittère et ne laisse aucun caractère douteux", () => {
  expect(slugifyHeading("Mesurer : le « texte » !")).toBe("mesurer-le-texte");
  expect(slugifyHeading("Déjà vu")).toBe("deja-vu");
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test scripts/markdown.test.ts` Expected: FAIL —
`Cannot find module './markdown.ts'`

- [x] **Step 3: Implémenter**

```ts
/*
 * Markdown → HTML, en deux temps.
 *
 * 1. `Bun.markdown.html()` fait le rendu GFM. Il est natif, donc le blog n'a
 *    aucune dépendance de rendu — mais il laisse passer le HTML brut, d'où le
 *    garde-fou ci-dessous.
 * 2. Une passe `HTMLRewriter` applique ce que le rendu ne fait pas : ancres de
 *    titres, `rel` sur les liens externes, tables défilables, images
 *    paresseuses, et le marquage RGAA des runs chinois.
 *
 * Le marquage CJK passe ici par le gestionnaire de nœuds texte, et non par une
 * expression régulière sur une chaîne échappée comme dans src/render.ts : il ne
 * peut donc structurellement pas atteindre un attribut ou un nom de balise.
 */

import type { Lang } from "../src/translations.ts";

/** CJK idéographique et ponctuation CJK — les runs qui doivent déclarer leur langue. */
const CJK_RUN = /[　-〿㐀-䶿一-鿿豈-﫿]+/g;

/** Ce qu'un article ne doit jamais contenir : exécutable ou cadre embarqué. */
const DANGEROUS = /<\s*(script|iframe|object|embed)\b|\son[a-z]+\s*=/i;

export function assertSafeMarkdown(source: string, path: string): void {
  const match = DANGEROUS.exec(source);
  if (match) {
    throw new Error(
      `${path}: HTML interdit dans une source d'article (${
        match[0].trim()
      }). ` +
        "Les articles sont du Markdown ; le HTML exécutable n'y a pas sa place.",
    );
  }
}

export function slugifyHeading(text: string): string {
  return text
    .normalize("NFD")
    .replace(/\p{Diacritic}/gu, "")
    .toLowerCase()
    .replace(/[^\p{Letter}\p{Number}]+/gu, "-")
    .replace(/^-+|-+$/g, "");
}

export async function renderMarkdown(
  body: string,
  lang: Lang,
): Promise<string> {
  const rendered = Bun.markdown.html(body);
  const seen = new Map<string, number>();
  const chinese = !lang.startsWith("zh");

  return await new HTMLRewriter()
    .on("h2, h3, h4", {
      text(chunk) {
        // Le texte arrive par morceaux ; on accumule sur le handler d'élément.
        pending += chunk.text;
      },
      element(el) {
        pending = "";
        el.onEndTag(() => {
          const base = slugifyHeading(pending) || "section";
          const count = (seen.get(base) ?? 0) + 1;
          seen.set(base, count);
          el.setAttribute("id", count === 1 ? base : `${base}-${count}`);
        });
      },
    })
    .on("a[href]", {
      element(el) {
        const href = el.getAttribute("href") ?? "";
        if (/^https?:\/\//i.test(href)) {
          el.setAttribute("rel", "noopener noreferrer");
        }
      },
    })
    .on("table", {
      element(el) {
        el.before('<div class="table-scroll">', { html: true });
        el.after("</div>", { html: true });
      },
    })
    .on("img", {
      element(el) {
        el.setAttribute("loading", "lazy");
        el.setAttribute("decoding", "async");
      },
    })
    .on("p, li, td, th, h2, h3, h4, blockquote", {
      text(chunk) {
        if (!chinese || !CJK_RUN.test(chunk.text)) return;
        CJK_RUN.lastIndex = 0;
        chunk.replace(
          chunk.text.replace(
            CJK_RUN,
            (run) => `<span lang="zh-Hans">${run}</span>`,
          ),
          { html: true },
        );
      },
    })
    .transform(new Response(rendered))
    .text();
}

let pending = "";
```

> **Note pour l'implémenteur :** `pending` en variable de module est un piège de
> concurrence. Si `renderMarkdown` peut être appelée en parallèle (elle le sera
> : `scripts/content.ts` charge les articles avec `Promise.all`), remplace-la
> par une accumulation locale à l'appel — par exemple un `Map<Element, string>`
> créé dans le corps de la fonction, ou une première passe qui collecte les
> textes de titres avant la passe de réécriture. **Écris le test de concurrence
> avant de choisir** : deux `renderMarkdown` concurrents avec des titres
> différents doivent produire les bons ids.

- [x] **Step 4: Ajouter le test de concurrence et corriger l'accumulation**

```ts
test("deux rendus concurrents ne mélangent pas leurs ids de titres", async () => {
  const [a, b] = await Promise.all([
    renderMarkdown("## Alpha", "fr"),
    renderMarkdown("## Beta", "fr"),
  ]);

  expect(a).toContain('id="alpha"');
  expect(a).not.toContain('id="beta"');
  expect(b).toContain('id="beta"');
  expect(b).not.toContain('id="alpha"');
});
```

- [x] **Step 5: Lancer les tests et vérifier qu'ils passent**

Run: `bun test scripts/markdown.test.ts && bun run check` Expected: PASS, y
compris le test de concurrence.

- [x] **Step 6: Commit**

```bash
deno fmt scripts/markdown.ts scripts/markdown.test.ts && deno lint
git add scripts/markdown.ts scripts/markdown.test.ts
git commit -m "feat: pipeline Markdown natif Bun avec réécriture HTMLRewriter"
```

---

### Task 3: URLs et profondeurs

**Files:**

- Create: `src/urls.ts`
- Test: `src/urls.test.ts`

**Interfaces:**

- Consumes: `Lang`, `LANGS` de `src/translations.ts`.
- Produces:
  ```ts
  export type PageRef =
    | { kind: "home"; lang: Lang }
    | { kind: "cv"; lang: Lang }
    | { kind: "blogIndex"; lang: Lang }
    | { kind: "post"; lang: Lang; slug: string };

  export const RESERVED_SLUGS: ReadonlySet<string>;
  export function rel(depth: number): string;
  export function pagePath(ref: PageRef): string; // relatif à dist/
  export function pageDepth(ref: PageRef): number;
  export function hrefTo(from: PageRef, to: PageRef): string;
  export function assertSlug(slug: string, path: string): void;
  ```

- [x] **Step 1: Écrire les tests qui échouent**

```ts
/*
 * La disposition met l'anglais à la racine et chaque autre langue dans son
 * dossier. La profondeur cesse donc d'être uniforme, et tout chemin d'asset ou
 * de lien se calcule. Ces tests sont ce qui empêche un `../` de trop.
 */

import { expect, test } from "bun:test";
import {
  assertSlug,
  hrefTo,
  pageDepth,
  pagePath,
  rel,
  RESERVED_SLUGS,
} from "./urls.ts";

test("rel(depth) remonte à la racine du site", () => {
  expect(rel(0)).toBe("./");
  expect(rel(1)).toBe("../");
  expect(rel(2)).toBe("../../");
});

test.each([
  [{ kind: "home", lang: "en" } as const, "index.html", 0],
  [{ kind: "cv", lang: "en" } as const, "cv.html", 0],
  [{ kind: "blogIndex", lang: "en" } as const, "blog/index.html", 1],
  [
    { kind: "post", lang: "en", slug: "mesurer" } as const,
    "blog/mesurer.html",
    1,
  ],
  [{ kind: "home", lang: "fr" } as const, "fr/index.html", 1],
  [{ kind: "cv", lang: "fr" } as const, "fr/cv.html", 1],
  [{ kind: "blogIndex", lang: "fr" } as const, "fr/blog/index.html", 2],
  [
    { kind: "post", lang: "zh-hant", slug: "mesurer" } as const,
    "zh-hant/blog/mesurer.html",
    2,
  ],
])("pagePath/%o", (ref, path, depth) => {
  expect(pagePath(ref)).toBe(path);
  expect(pageDepth(ref)).toBe(depth);
});

test("un lien croisé remonte puis redescend", () => {
  const from = { kind: "post", lang: "fr", slug: "mesurer" } as const;
  expect(hrefTo(from, { kind: "home", lang: "en" })).toBe("../../index.html");
  expect(hrefTo(from, { kind: "cv", lang: "zh" })).toBe("../../zh/cv.html");
  expect(hrefTo(from, { kind: "post", lang: "en", slug: "mesurer" }))
    .toBe("../../blog/mesurer.html");
});

test("un lien depuis la racine reste local", () => {
  const from = { kind: "home", lang: "en" } as const;
  expect(hrefTo(from, { kind: "cv", lang: "en" })).toBe("./cv.html");
  expect(hrefTo(from, { kind: "blogIndex", lang: "fr" }))
    .toBe("./fr/blog/index.html");
});

test.each([...RESERVED_SLUGS])("le slug réservé « %s » est refusé", (slug) => {
  expect(() => assertSlug(slug, `content/posts/${slug}/fr.md`)).toThrow(slug);
});

test("les codes de langue sont réservés", () => {
  expect(() => assertSlug("zh-hant", "p")).toThrow();
});

test.each(["Mesurer", "mon article", "a/b", "", "é"])(
  "un slug hors [a-z0-9-] est refusé : %s",
  (slug) => {
    expect(() => assertSlug(slug, "p")).toThrow();
  },
);

test("un slug ordinaire passe", () => {
  expect(() => assertSlug("mesurer-le-texte", "p")).not.toThrow();
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test src/urls.test.ts` Expected: FAIL —
`Cannot find module './urls.ts'`

- [x] **Step 3: Implémenter**

```ts
/*
 * Où vit chaque page, et comment on va de l'une à l'autre.
 *
 * L'anglais est à la racine — ce que `langUrl` encodait déjà — et chaque autre
 * langue prend un dossier. La profondeur n'est donc plus uniforme, et tous les
 * chemins d'assets comme les liens inter-pages passent par `rel(depth)`. Ils
 * restent relatifs, donc le `dist/` se dépose à n'importe quel préfixe de
 * bucket : c'est l'invariant que le repo tient depuis le début.
 */

import { type Lang, LANGS } from "./translations.ts";

export type PageRef =
  | { kind: "home"; lang: Lang }
  | { kind: "cv"; lang: Lang }
  | { kind: "blogIndex"; lang: Lang }
  | { kind: "post"; lang: Lang; slug: string };

/**
 * Les noms qu'un slug ne peut pas prendre : ils désignent déjà un fichier ou un
 * dossier du site. Les codes de langue en font partie — un article « fr »
 * écraserait le dossier de la langue.
 */
export const RESERVED_SLUGS: ReadonlySet<string> = new Set([
  "assets",
  "blog",
  "cv",
  "index",
  "404",
  "robots",
  "sitemap",
  "feed",
  "og-image",
  ...LANGS,
]);

const SLUG = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;

export function assertSlug(slug: string, path: string): void {
  if (RESERVED_SLUGS.has(slug)) {
    throw new Error(`${path}: « ${slug} » est un nom réservé du site`);
  }
  if (!SLUG.test(slug)) {
    throw new Error(
      `${path}: slug invalide « ${slug} » — attendu [a-z0-9] séparés par des tirets`,
    );
  }
}

export function rel(depth: number): string {
  return depth === 0 ? "./" : "../".repeat(depth);
}

/** Le chemin du fichier dans `dist/`, sans barre oblique de tête. */
export function pagePath(ref: PageRef): string {
  const prefix = ref.lang === "en" ? "" : `${ref.lang}/`;
  switch (ref.kind) {
    case "home":
      return `${prefix}index.html`;
    case "cv":
      return `${prefix}cv.html`;
    case "blogIndex":
      return `${prefix}blog/index.html`;
    case "post":
      return `${prefix}blog/${ref.slug}.html`;
  }
}

/** Le nombre de dossiers entre la page et la racine du site. */
export function pageDepth(ref: PageRef): number {
  return pagePath(ref).split("/").length - 1;
}

/** Un lien relatif d'une page vers une autre, valable à n'importe quel préfixe. */
export function hrefTo(from: PageRef, to: PageRef): string {
  return `${rel(pageDepth(from))}${pagePath(to)}`;
}
```

- [x] **Step 4: Lancer les tests et vérifier qu'ils passent**

Run: `bun test src/urls.test.ts && bun run check` Expected: PASS.

- [x] **Step 5: Commit**

```bash
deno fmt src/urls.ts src/urls.test.ts && deno lint
git add src/urls.ts src/urls.test.ts
git commit -m "feat: chemins et liens de pages avec profondeur relative"
```

---

### Task 4: Métadonnées d'article et sélection

**Files:**

- Create: `src/post.ts`
- Test: `src/post.test.ts`

**Interfaces:**

- Consumes: `Lang` de `src/translations.ts`.
- Produces:
  ```ts
  export interface PostMeta {
    slug: string;
    lang: Lang;
    title: string;
    date: string;
    updated?: string;
    summary: string;
    tags: readonly string[];
  }
  export const HOME_POST_COUNT = 5;
  export function byLang(posts: readonly PostMeta[], lang: Lang): PostMeta[];
  export function langsOf(posts: readonly PostMeta[], slug: string): Lang[];
  export function deriveSummary(text: string): string;
  ```

**Pourquoi ce module est dans `src/` et non `scripts/`** : c'est le seul état
d'article que le client reçoit. Les corps, le HTML et les chemins disque restent
dans `scripts/content.ts`. La frontière est structurelle, pas conventionnelle.

- [x] **Step 1: Écrire les tests qui échouent**

```ts
import { expect, test } from "bun:test";
import { byLang, deriveSummary, langsOf, type PostMeta } from "./post.ts";

const post = (
  slug: string,
  lang: PostMeta["lang"],
  date: string,
): PostMeta => ({
  slug,
  lang,
  title: `${slug} (${lang})`,
  date,
  summary: "…",
  tags: [],
});

const CORPUS: PostMeta[] = [
  post("a", "fr", "2026-01-01"),
  post("a", "en", "2026-01-01"),
  post("b", "fr", "2026-03-01"),
  post("c", "zh", "2026-02-01"),
];

test("l'index d'une langue ne contient que ses articles, du plus récent au plus ancien", () => {
  expect(byLang(CORPUS, "fr").map((p) => p.slug)).toEqual(["b", "a"]);
  expect(byLang(CORPUS, "zh").map((p) => p.slug)).toEqual(["c"]);
  expect(byLang(CORPUS, "es")).toEqual([]);
});

test("langsOf donne les langues où l'article existe, dans l'ordre du site", () => {
  expect(langsOf(CORPUS, "a")).toEqual(["en", "fr"]);
  expect(langsOf(CORPUS, "c")).toEqual(["zh"]);
  expect(langsOf(CORPUS, "inconnu")).toEqual([]);
});

test("deriveSummary coupe à la fin d'une phrase quand il y en a une", () => {
  const text = "Première phrase. Deuxième phrase, plus longue que la première.";
  expect(deriveSummary(text)).toBe("Première phrase.");
});

test("deriveSummary tronque proprement un texte sans ponctuation utile", () => {
  const text = "mot ".repeat(100).trim();
  const summary = deriveSummary(text);

  expect(summary.length).toBeLessThanOrEqual(201);
  expect(summary.endsWith("…")).toBe(true);
  expect(summary).not.toContain("mot mot…"); // coupe sur un mot entier
});

test("deriveSummary rend un texte court tel quel", () => {
  expect(deriveSummary("Court")).toBe("Court");
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test src/post.test.ts` Expected: FAIL —
`Cannot find module './post.ts'`

- [x] **Step 3: Implémenter**

```ts
/*
 * Les métadonnées d'un article, et les seules opérations que le rendu en fait.
 *
 * Ce module est la frontière : c'est tout ce qu'un article expose au client.
 * Le corps Markdown, le HTML rendu et le chemin disque vivent dans
 * scripts/content.ts et ne franchissent jamais cette ligne — un blog n'a aucune
 * raison d'embarquer ses articles dans le bundle.
 */

import { type Lang, LANGS } from "./translations.ts";

export interface PostMeta {
  slug: string;
  lang: Lang;
  title: string;
  date: string; // YYYY-MM-DD
  updated?: string;
  summary: string;
  tags: readonly string[];
}

/** Combien d'articles l'accueil montre avant de renvoyer vers l'index. */
export const HOME_POST_COUNT = 5;

const SUMMARY_MAX = 200;

/** Les articles d'une langue, du plus récent au plus ancien. */
export function byLang(posts: readonly PostMeta[], lang: Lang): PostMeta[] {
  return posts
    .filter((post) => post.lang === lang)
    .sort((a, b) => (a.date < b.date ? 1 : a.date > b.date ? -1 : 0));
}

/** Les langues où un article existe, dans l'ordre canonique du site. */
export function langsOf(posts: readonly PostMeta[], slug: string): Lang[] {
  const present = new Set(
    posts.filter((post) => post.slug === slug).map((post) => post.lang),
  );
  return LANGS.filter((lang) => present.has(lang));
}

/**
 * Un résumé quand le frontmatter n'en donne pas. On préfère finir sur une
 * phrase entière ; à défaut on tronque sur un mot, jamais au milieu.
 */
export function deriveSummary(text: string): string {
  const clean = text.replace(/\s+/g, " ").trim();
  if (clean.length <= SUMMARY_MAX) return clean;

  const head = clean.slice(0, SUMMARY_MAX);
  const sentence = Math.max(
    head.lastIndexOf(". "),
    head.lastIndexOf("。"),
    head.lastIndexOf("！"),
    head.lastIndexOf("? "),
  );
  if (sentence > SUMMARY_MAX / 3) return clean.slice(0, sentence + 1).trim();

  const word = head.lastIndexOf(" ");
  return `${(word > 0 ? head.slice(0, word) : head).trim()}…`;
}
```

- [x] **Step 4: Lancer les tests et vérifier qu'ils passent**

Run: `bun test src/post.test.ts && bun run check` Expected: PASS.

- [x] **Step 5: Commit**

```bash
deno fmt src/post.ts src/post.test.ts && deno lint
git add src/post.ts src/post.test.ts
git commit -m "feat: métadonnées d'article et sélection par langue"
```

---

### Task 5: Chargement du contenu depuis le disque

**Files:**

- Create: `scripts/content.ts`
- Test: `scripts/content.test.ts`
- Modify: `src/translations.ts` (exporter `toHongKongText`)

**Interfaces:**

- Consumes: `parseFrontmatter` (T1), `assertSafeMarkdown` + `renderMarkdown`
  (T2), `assertSlug` (T3), `PostMeta` + `deriveSummary` (T4).
- Produces:
  ```ts
  export interface Post extends PostMeta {
    body: string;
    html: string;
    text: string;
    sourcePath: string;
  }
  export async function loadPosts(root?: string): Promise<Post[]>;
  ```
  et depuis `src/translations.ts` :
  ```ts
  export function toHongKongText(text: string): string;
  ```

**Règle de projection `zh-hk`** (comble un implicite du spec) : un article qui a
`zh-hant.md` sans `zh-hk.md` voit sa version HK **dérivée** par le lexique,
comme le CV. Un `zh-hk.md` explicite l'emporte.

- [x] **Step 1: Extraire `toHongKongText` dans `src/translations.ts`**

Remplacer la fonction privée `toHongKong` (fin de fichier) par une paire dont la
version texte est exportée, pour que les articles utilisent le même lexique que
le CV — un seul endroit à tenir :

```ts
/**
 * Le lexique Taïwan → Hong Kong appliqué à du texte brut. Exporté parce que les
 * articles du blog en dépendent aussi : une projection qui vivrait en double
 * dériverait, et la page HK afficherait du vocabulaire taïwanais sans que rien
 * n'échoue.
 */
export function toHongKongText(text: string): string {
  let out = text;
  for (const [taiwan, hongKong] of Object.entries(HK_LEXICON)) {
    out = out.replaceAll(taiwan, hongKong);
  }
  return out;
}

function toHongKong(t: Translation): Translation {
  return JSON.parse(toHongKongText(JSON.stringify(t))) as Translation;
}
```

- [x] **Step 2: Vérifier que la refonte ne change rien**

Run: `bun test src/translations.test.ts` Expected: PASS — les quatre tests de
projection existants valident déjà que le lexique s'applique et que la structure
survit.

- [x] **Step 3: Écrire les tests de chargement qui échouent**

```ts
/*
 * Le chargement lit un vrai disque, donc chaque test construit son propre
 * corpus dans un dossier temporaire : pas de fixture partagée à faire dériver,
 * et l'arborescence testée est exactement celle que le build lira.
 */

import { afterEach, expect, test } from "bun:test";
import { mkdtemp, rm } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { loadPosts } from "./content.ts";

const roots: string[] = [];

afterEach(async () => {
  await Promise.all(
    roots.splice(0).map((r) => rm(r, { recursive: true, force: true })),
  );
});

async function corpus(
  files: Record<string, string>,
): Promise<string> {
  const root = await mkdtemp(join(tmpdir(), "cv-content-"));
  roots.push(root);
  for (const [path, source] of Object.entries(files)) {
    await Bun.write(join(root, "posts", path), source);
  }
  return root;
}

const article = (title: string, extra = "") =>
  ["---", `title: ${title}`, "date: 2026-08-08", extra, "---", "", "Le corps."]
    .filter(Boolean)
    .join("\n");

test("découvre un article par langue et rend son HTML", async () => {
  const root = await corpus({
    "mesurer/fr.md": article("Mesurer"),
    "mesurer/en.md": article("Measuring"),
  });

  const posts = await loadPosts(root);

  expect(posts).toHaveLength(2);
  expect(posts.map((p) => p.lang).sort()).toEqual(["en", "fr"]);
  expect(posts[0]!.slug).toBe("mesurer");
  expect(posts.every((p) => p.html.includes("<p>Le corps.</p>"))).toBe(true);
  expect(posts.every((p) => p.text.includes("Le corps."))).toBe(true);
});

test("les brouillons sont exclus, sauf sous DRAFTS=1", async () => {
  const root = await corpus({
    "publie/fr.md": article("Publié"),
    "brouillon/fr.md": article("Brouillon", "draft: true"),
  });

  expect((await loadPosts(root)).map((p) => p.slug)).toEqual(["publie"]);

  process.env.DRAFTS = "1";
  try {
    expect((await loadPosts(root)).map((p) => p.slug).sort())
      .toEqual(["brouillon", "publie"]);
  } finally {
    delete process.env.DRAFTS;
  }
});

test("zh-hk est dérivé de zh-hant par projection lexicale", async () => {
  const root = await corpus({
    "x/zh-hant.md": [
      "---",
      "title: 軟體與網路",
      "date: 2026-08-08",
      "---",
      "",
      "談談軟體。",
    ].join("\n"),
  });

  const posts = await loadPosts(root);
  const hk = posts.find((p) => p.lang === "zh-hk");

  expect(hk).toBeDefined();
  expect(hk!.title).toBe("軟件與網絡");
  expect(hk!.html).toContain("軟件");
  expect(hk!.html).not.toContain("軟體");
});

test("un zh-hk explicite l'emporte sur la projection", async () => {
  const root = await corpus({
    "x/zh-hant.md": article("台灣版"),
    "x/zh-hk.md": article("香港版"),
  });

  const hk = (await loadPosts(root)).find((p) => p.lang === "zh-hk");
  expect(hk!.title).toBe("香港版");
});

test("le résumé vient du frontmatter, sinon du texte", async () => {
  const root = await corpus({
    "a/fr.md": article("A", "summary: Écrit à la main."),
    "b/fr.md": article("B"),
  });

  const posts = await loadPosts(root);
  expect(posts.find((p) => p.slug === "a")!.summary).toBe("Écrit à la main.");
  expect(posts.find((p) => p.slug === "b")!.summary).toBe("Le corps.");
});

test("un slug réservé casse le chargement", async () => {
  const root = await corpus({ "cv/fr.md": article("Collision") });
  expect(loadPosts(root)).rejects.toThrow("cv");
});

test("un nom de fichier qui n'est pas une langue casse le chargement", async () => {
  const root = await corpus({ "x/de.md": article("Deutsch") });
  expect(loadPosts(root)).rejects.toThrow("de");
});

test("du HTML dangereux casse le chargement, avec le chemin", async () => {
  const root = await corpus({
    "x/fr.md": [
      "---",
      "title: T",
      "date: 2026-08-08",
      "---",
      "",
      "<script>alert(1)</script>",
    ].join("\n"),
  });

  expect(loadPosts(root)).rejects.toThrow("x/fr.md");
});
```

- [x] **Step 4: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test scripts/content.test.ts` Expected: FAIL —
`Cannot find module './content.ts'`

- [x] **Step 5: Implémenter**

```ts
/*
 * Le corpus, lu depuis `content/posts/<slug>/<lang>.md`.
 *
 * Le système de fichiers EST la donnée : le nom du dossier est le slug, le nom
 * du fichier est la langue. Il n'y a donc aucune table de correspondance à
 * tenir, et la règle « un article existe dans 1..n langues » ne peut pas être
 * violée par oubli.
 *
 * `zh-hk` est l'exception, et elle suit le CV : la page de Hong Kong est une
 * projection lexicale de celle de Taïwan (voir toHongKongText dans
 * src/translations.ts), pas une traduction. Un `zh-hk.md` écrit à la main
 * l'emporte sur la projection.
 */

import { basename, dirname, join } from "node:path";
import { isLang, type Lang, toHongKongText } from "../src/translations.ts";
import { deriveSummary, type PostMeta } from "../src/post.ts";
import { assertSlug } from "../src/urls.ts";
import { parseFrontmatter } from "./frontmatter.ts";
import { assertSafeMarkdown, renderMarkdown } from "./markdown.ts";

export interface Post extends PostMeta {
  body: string;
  html: string;
  text: string;
  sourcePath: string;
}

const DEFAULT_ROOT = "content";

async function readPost(
  root: string,
  file: string,
  lang: Lang,
  slug: string,
): Promise<Post> {
  const sourcePath = join(root, "posts", slug, file);
  const source = await Bun.file(sourcePath).text();
  assertSafeMarkdown(source, sourcePath);
  const { data, body } = parseFrontmatter(source, sourcePath);
  const text = Bun.markdown.render(body);

  return {
    slug,
    lang,
    title: data.title,
    date: data.date,
    ...(data.updated ? { updated: data.updated } : {}),
    summary: data.summary ?? deriveSummary(text),
    tags: data.tags,
    body,
    html: await renderMarkdown(body, lang),
    text,
    sourcePath,
    // `draft` n'apparaît pas : un Post construit est publiable.
  };
}

/** La version Hong Kong d'un article de Taïwan, par le lexique du CV. */
async function projectHongKong(taiwan: Post): Promise<Post> {
  const body = toHongKongText(taiwan.body);
  return {
    ...taiwan,
    lang: "zh-hk",
    title: toHongKongText(taiwan.title),
    summary: toHongKongText(taiwan.summary),
    body,
    html: await renderMarkdown(body, "zh-hk"),
    text: toHongKongText(taiwan.text),
  };
}

export async function loadPosts(root = DEFAULT_ROOT): Promise<Post[]> {
  const glob = new Bun.Glob("*/*.md");
  const found: { file: string; lang: Lang; slug: string }[] = [];

  for await (const entry of glob.scan(join(root, "posts"))) {
    const file = basename(entry);
    const slug = dirname(entry);
    const lang = file.replace(/\.md$/, "");
    const sourcePath = join(root, "posts", entry);

    assertSlug(slug, sourcePath);
    if (!isLang(lang)) {
      throw new Error(
        `${sourcePath}: « ${lang} » n'est pas une langue du site`,
      );
    }
    found.push({ file, lang, slug });
  }

  const drafts = process.env.DRAFTS === "1";
  const all = await Promise.all(
    found.map(async ({ file, lang, slug }) => {
      const sourcePath = join(root, "posts", slug, file);
      const source = await Bun.file(sourcePath).text();
      const { data } = parseFrontmatter(source, sourcePath);
      return data.draft && !drafts
        ? null
        : await readPost(root, file, lang, slug);
    }),
  );

  const posts = all.filter((post): post is Post => post !== null);

  // Projection HK : seulement pour les articles qui n'en ont pas d'explicite.
  const explicit = new Set(
    posts.filter((p) => p.lang === "zh-hk").map((p) => p.slug),
  );
  const projected = await Promise.all(
    posts
      .filter((p) => p.lang === "zh-hant" && !explicit.has(p.slug))
      .map(projectHongKong),
  );

  return [...posts, ...projected];
}
```

> **Note pour l'implémenteur :** l'implémentation ci-dessus lit et parse chaque
> fichier deux fois (une fois pour le filtre `draft`, une fois dans `readPost`).
> C'est volontairement naïf pour rester lisible. Si le corpus grandit, fusionne
> les deux passes — mais **seulement** avec le test de brouillon toujours vert.

- [x] **Step 6: Lancer les tests et vérifier qu'ils passent**

Run:
`bun test scripts/content.test.ts src/translations.test.ts && bun run check`
Expected: PASS.

- [x] **Step 7: Commit**

```bash
deno fmt scripts/content.ts scripts/content.test.ts src/translations.ts && deno lint
git add scripts/content.ts scripts/content.test.ts src/translations.ts
git commit -m "feat: chargement des articles depuis le disque avec projection zh-hk"
```

---

### Task 6: Chaînes d'interface du blog

**Files:**

- Modify: `src/translations.ts`
- Test: `src/translations.test.ts` (les tests de projection existants couvrent
  déjà la parité de structure — rien à ajouter)

**Interfaces:**

- Produces: `Translation["blog"]` et deux entrées `nav` supplémentaires.

- [x] **Step 1: Ajouter le bloc `blog` à l'objet `en`**

`en` est la source de vérité : son type inféré **est** `Translation`, donc
ajouter ces clés rend les six autres objets non conformes jusqu'à ce qu'ils
soient complétés. C'est le mécanisme de parité du fichier, et il fait le travail
de rappel à notre place.

```ts
nav: {
  // … clés existantes …
  cv: "CV",
  writing: "Writing",
},
blog: {
  indexTitle: "Writing",
  indexIntro: "Notes on measurement, interfaces and the languages I work in.",
  latest: "Latest writing",
  all: "All writing",
  readMore: "Read",
  published: "Published",
  updated: "Updated",
  backToIndex: "Back to all writing",
  empty: "Nothing published in this language yet.",
  // Lu par un lecteur d'écran sur les langues où l'article n'existe pas.
  notInLanguage: "This article is not available in this language",
  tags: "Tags",
},
```

- [x] **Step 2: Vérifier que le typecheck échoue pour les six autres langues**

Run: `bun run check` Expected: FAIL — six erreurs, une par traduction, chacune
nommant `blog`.

- [x] **Step 3: Compléter `fr`, `pt`, `es`, `zh`, `zh-hant`**

`zh-hk` est dérivé par projection et ne s'écrit pas à la main.

```ts
// fr
  nav: { /* … */ cv: "CV", writing: "Écrits" },
  blog: {
    indexTitle: "Écrits",
    indexIntro:
      "Notes sur la mesure, les interfaces et les langues que je pratique.",
    latest: "Derniers écrits",
    all: "Tous les écrits",
    readMore: "Lire",
    published: "Publié le",
    updated: "Mis à jour le",
    backToIndex: "Retour aux écrits",
    empty: "Rien de publié dans cette langue pour l'instant.",
    notInLanguage: "Cet article n'est pas disponible dans cette langue",
    tags: "Mots-clés",
  },

// pt — portugais européen, orthographe PRÉ-1990 (« acção », « contacto »)
  nav: { /* … */ cv: "CV", writing: "Escritos" },
  blog: {
    indexTitle: "Escritos",
    indexIntro:
      "Notas sobre medição, interfaces e as línguas em que trabalho.",
    latest: "Últimos escritos",
    all: "Todos os escritos",
    readMore: "Ler",
    published: "Publicado a",
    updated: "Actualizado a",
    backToIndex: "Voltar aos escritos",
    empty: "Ainda nada publicado nesta língua.",
    notInLanguage: "Este artigo não está disponível nesta língua",
    tags: "Palavras-chave",
  },

// es — espagnol péninsulaire
  nav: { /* … */ cv: "CV", writing: "Escritos" },
  blog: {
    indexTitle: "Escritos",
    indexIntro:
      "Notas sobre medición, interfaces y los idiomas en los que trabajo.",
    latest: "Últimos escritos",
    all: "Todos los escritos",
    readMore: "Leer",
    published: "Publicado el",
    updated: "Actualizado el",
    backToIndex: "Volver a los escritos",
    empty: "Todavía no hay nada publicado en este idioma.",
    notInLanguage: "Este artículo no está disponible en este idioma",
    tags: "Etiquetas",
  },

// zh — simplifié
  nav: { /* … */ cv: "简历", writing: "文章" },
  blog: {
    indexTitle: "文章",
    indexIntro: "关于测量、界面，以及我使用的几种语言的笔记。",
    latest: "最新文章",
    all: "全部文章",
    readMore: "阅读",
    published: "发布于",
    updated: "更新于",
    backToIndex: "返回文章列表",
    empty: "这个语言暂时还没有文章。",
    notInLanguage: "本文暂无此语言版本",
    tags: "标签",
  },

// zh-hant — usage de Taïwan (軟體, 專案)
  nav: { /* … */ cv: "履歷", writing: "文章" },
  blog: {
    indexTitle: "文章",
    indexIntro: "關於測量、介面，以及我使用的幾種語言的筆記。",
    latest: "最新文章",
    all: "全部文章",
    readMore: "閱讀",
    published: "發佈於",
    updated: "更新於",
    backToIndex: "返回文章列表",
    empty: "這個語言目前還沒有文章。",
    notInLanguage: "本文暫無此語言版本",
    tags: "標籤",
  },
```

- [x] **Step 4: Vérifier le typecheck et les tests de projection**

Run: `bun run check && bun test src/translations.test.ts` Expected: PASS — la
structure `zh-hk` reste identique à `zh-hant`.

- [x] **Step 5: Régénérer les sous-ensembles de polices**

Run: `bun run fonts:update` Expected: les `.woff2` sont réécrits ; les nouveaux
glyphes (履歷, 標籤, 發佈…) sont couverts.

- [x] **Step 6: Commit**

```bash
deno fmt src/translations.ts && deno lint
git add src/translations.ts src/fonts/
git commit -m "feat: chaînes d'interface du blog dans les sept langues"
```

---

### Task 7: Découper `render.ts` sans changer son comportement

**Files:**

- Create: `src/render/shell.ts`, `src/render/cv.ts`, `src/render/blog.ts`
- Modify: `src/render.ts` (devient une façade)
- Test: `src/render.test.ts` (inchangé — c'est le filet)

**Interfaces:**

- Produces: `src/render.ts` continue d'exporter exactement ce qu'il exportait
  (`langUrl`, `langFromPath`, `pageLang`, `pageTitle`, `NAV_LINKS`,
  `STORAGE_LANG_KEY`, `languageNegotiationScript`, `renderApp`, `Theme`), plus
  les nouveaux modules.

**C'est un refactor pur.** Aucune sortie ne change. Le test existant
`src/render.test.ts` et `scripts/build.test.ts` sont la preuve.

- [x] **Step 1: Capturer la sortie actuelle comme référence**

```ts
// src/render.snapshot.test.ts — temporaire, supprimé à l'étape 5
import { expect, test } from "bun:test";
import { renderApp } from "./render.ts";
import { LANGS } from "./translations.ts";

test.each([...LANGS])(
  "le markup de %s ne change pas pendant le découpage",
  (lang) => {
    expect(renderApp(lang, "light")).toMatchSnapshot();
  },
);
```

- [x] **Step 2: Enregistrer la référence**

Run: `bun test src/render.snapshot.test.ts` Expected: PASS, sept instantanés
écrits.

- [x] **Step 3: Déplacer le code, sans le modifier**

- `src/render/shell.ts` ← `nav`, `section`, `phoneChrome`, `markChinese`,
  `CJK_RUN`, `NAV_LINKS`, `Theme`.
- `src/render/cv.ts` ← `hero`, `about`, `STATS`, `TimelineEntry`,
  `timelineItem`, `timeline`, `EXPERIENCE`, `experience`, `education`,
  `certifications`, `skills`, `hobbies`, `dialogue`, `contact`.
- `src/render/blog.ts` ← vide pour l'instant (Task 8).
- `src/render.ts` conserve `langUrl`, `langFromPath`, `pageLang`,
  `STORAGE_LANG_KEY`, `languageNegotiationScript`, `pageTitle`, `renderApp`, et
  ré-exporte le reste.

- [x] **Step 4: Vérifier que rien n'a bougé**

Run: `bun test && bun run check` Expected: PASS — les sept instantanés sont
identiques. **Un instantané qui change signale un déplacement fautif, pas un
instantané à mettre à jour.**

- [x] **Step 5: Supprimer le filet temporaire**

```bash
rm src/render.snapshot.test.ts
rm -rf src/__snapshots__
bun test
```

- [x] **Step 6: Commit**

```bash
deno fmt src/render.ts src/render/ && deno lint
git add src/render.ts src/render/
git commit -m "refactor: découper render.ts en shell, cv et blog derrière une façade"
```

---

### Task 8: Rendu des pages du blog

**Files:**

- Modify: `src/render/blog.ts`, `src/render.ts` (exporter `renderPage`)
- Test: `src/render/blog.test.ts`

**Interfaces:**

- Consumes: `PostMeta`, `byLang`, `langsOf`, `HOME_POST_COUNT` (T4) ; `PageRef`,
  `hrefTo` (T3) ; `nav`, `section` (T7).
- Produces:
  ```ts
  export type Page =
    | { kind: "home"; posts: readonly PostMeta[] }
    | { kind: "cv" }
    | { kind: "blogIndex"; posts: readonly PostMeta[] }
    | {
      kind: "post";
      post: PostMeta;
      html: string;
      posts: readonly PostMeta[];
    };

  export function renderPage(page: Page, lang: Lang, theme: Theme): string;
  ```

- [x] **Step 1: Écrire les tests qui échouent**

```ts
import { expect, test } from "bun:test";
import { renderPage } from "../render.ts";
import type { PostMeta } from "../post.ts";

const post = (
  slug: string,
  lang: PostMeta["lang"],
  date = "2026-08-08",
): PostMeta => ({
  slug,
  lang,
  title: `Titre ${slug} ${lang}`,
  date,
  summary: `Résumé ${slug}`,
  tags: ["bun"],
});

const POSTS: PostMeta[] = [
  post("a", "fr"),
  post("a", "en"),
  post("b", "fr", "2026-09-01"),
];

test("l'accueil montre le hero et les derniers écrits de la langue", () => {
  const html = renderPage({ kind: "home", posts: POSTS }, "fr", "light");

  expect(html).toContain("hero__name");
  expect(html).toContain("Derniers écrits");
  expect(html).toContain("Titre b fr");
  expect(html).not.toContain("Titre a en");
});

test("l'accueil d'une langue sans article le dit au lieu de mentir", () => {
  const html = renderPage({ kind: "home", posts: POSTS }, "es", "light");
  expect(html).toContain("Todavía no hay nada publicado");
});

test("l'index blog liste tous les articles de la langue, du plus récent au plus ancien", () => {
  const html = renderPage({ kind: "blogIndex", posts: POSTS }, "fr", "light");
  expect(html.indexOf("Titre b fr")).toBeLessThan(html.indexOf("Titre a fr"));
});

test("une page d'article porte son corps, sa date et un retour vers l'index", () => {
  const html = renderPage(
    {
      kind: "post",
      post: post("a", "fr"),
      html: "<p>Corps.</p>",
      posts: POSTS,
    },
    "fr",
    "light",
  );

  expect(html).toContain("<p>Corps.</p>");
  expect(html).toContain('<time datetime="2026-08-08"');
  expect(html).toContain("Retour aux écrits");
});

test("le sélecteur mène à la traduction quand elle existe", () => {
  const html = renderPage(
    { kind: "post", post: post("a", "fr"), html: "", posts: POSTS },
    "fr",
    "light",
  );
  expect(html).toContain('href="../../blog/a.html"');
});

test("sinon il mène à l'index de la langue, avec une mention audible", () => {
  const html = renderPage(
    { kind: "post", post: post("b", "fr"), html: "", posts: POSTS },
    "fr",
    "light",
  );

  expect(html).toContain('href="../../blog/index.html"');
  expect(html).toContain("Cet article n'est pas disponible dans cette langue");
});

test("le markup d'un article n'est jamais échappé, celui d'un titre l'est toujours", () => {
  const html = renderPage(
    {
      kind: "post",
      post: { ...post("a", "fr"), title: "5 < 6 & « x »" },
      html: "<p><em>vrai</em> markup</p>",
      posts: POSTS,
    },
    "fr",
    "light",
  );

  expect(html).toContain("<em>vrai</em>");
  expect(html).toContain("5 &lt; 6 &amp;");
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test src/render/blog.test.ts` Expected: FAIL — `renderPage` n'est pas
exporté.

- [x] **Step 3: Implémenter `src/render/blog.ts`**

Points non négociables, dérivés du spec :

- `post.html` est **déjà** du HTML sûr (Task 2 l'a produit et validé) : il est
  inséré tel quel. Tout le reste — titres, résumés, dates, tags — passe par
  `escapeHtml`.
- La liste est un `<ol role="list">`, comme la timeline du CV : l'ordre est
  signifiant, et `role="list"` restaure la sémantique que `list-style: none`
  retire dans Safari/VoiceOver.
- La date est un `<time datetime="YYYY-MM-DD">` dont le **texte visible est
  localisé par `Intl`**, jamais la date ISO brute :

  ```ts
  /*
   * La date lisible, dans la langue de la page. `HTML_LANG` porte déjà les
   * étiquettes BCP-47 du site (pt-PT, es-ES, zh-Hant-HK…) : on les réutilise
   * plutôt que d'ouvrir une seconde table qui dériverait. `timeZone: "UTC"`
   * est obligatoire — sans lui, une date sans heure est interprétée dans le
   * fuseau de la machine de build et recule d'un jour à l'ouest de Greenwich.
   */
  function formatDate(date: string, lang: Lang): string {
    return new Intl.DateTimeFormat(HTML_LANG[lang], {
      dateStyle: "long",
      timeZone: "UTC",
    }).format(new Date(`${date}T00:00:00Z`));
  }
  ```

  Vérifié : rend « 8 août 2026 », « 8 de agosto de 2026 », « 2026年8月8日 ». Le
  test correspondant assert la sortie `fr` et `zh`, pas seulement la présence
  d'un `<time>`.
- Le repli du sélecteur porte la mention en `.sr-only` — jamais en `aria-label`,
  puisqu'un attribut ne porte pas de langue (même règle que le sélecteur actuel,
  RGAA 8.7).

- [x] **Step 4: Lancer les tests et vérifier qu'ils passent**

Run: `bun test && bun run check` Expected: PASS, y compris les tests existants
du CV.

- [x] **Step 5: Commit**

```bash
deno fmt src/render/ src/render.ts && deno lint
git add src/render/ src/render.ts
git commit -m "feat: rendu de l'accueil, de l'index et des pages d'article"
```

---

### Task 9: Le build émet le nouveau jeu de pages

**Files:**

- Modify: `scripts/build.ts`
- Test: `scripts/build.test.ts`

**Interfaces:**

- Consumes: `loadPosts` (T5), `renderPage` (T8), `pagePath`/`pageDepth`/`rel`
  (T3), `byLang` (T4).

- [x] **Step 1: Écrire les tests qui échouent**

Ces tests s'ajoutent à `scripts/build.test.ts`, qui inspecte déjà le `dist/`
réellement produit :

```ts
test("chaque langue reçoit son accueil, son CV et son index blog", async () => {
  for (const lang of LANGS) {
    const prefix = lang === "en" ? "" : `${lang}/`;
    for (const file of ["index.html", "cv.html", "blog/index.html"]) {
      expect(await Bun.file(`dist/${prefix}${file}`).exists()).toBe(true);
    }
  }
});

test("les chemins d'assets sont relatifs à la profondeur de la page", async () => {
  const root = await Bun.file("dist/index.html").text();
  const nested = await Bun.file("dist/fr/blog/index.html").text();

  expect(root).toContain('"./assets/');
  expect(root).not.toContain('"../assets/');
  expect(nested).toContain('"../../assets/');
  expect(nested).not.toContain('"./assets/');
});

test("aucune page ne référence un asset absent", async () => {
  const glob = new Bun.Glob("**/*.html");
  for await (const page of glob.scan("dist")) {
    const html = await Bun.file(`dist/${page}`).text();
    const depth = page.split("/").length - 1;
    for (const [, href] of html.matchAll(/"((?:\.\.?\/)+assets\/[^"]+)"/g)) {
      const resolved = new URL(href, `file:///${page}`).pathname.slice(1);
      expect(
        await Bun.file(`dist/${resolved}`).exists(),
        `${page} (profondeur ${depth}) → ${href}`,
      ).toBe(true);
    }
  }
});

test("seule la racine négocie la langue", async () => {
  expect(await Bun.file("dist/index.html").text())
    .toContain("navigator.languages");
  for (const file of ["dist/fr/index.html", "dist/cv.html"]) {
    expect(await Bun.file(file).text()).not.toContain("navigator.languages");
  }
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun run build && bun test scripts/build.test.ts` Expected: FAIL — les
pages `cv.html` et `blog/` n'existent pas.

- [x] **Step 3: Adapter `scripts/build.ts`**

La boucle `for (const lang of LANGS)` cesse d'émettre une page par langue et
émet un `PageRef` par page. Le `<head>` par page est déjà construit par
`pageMeta` ; il gagne la profondeur pour ses chemins relatifs.

```ts
const posts = await loadPosts();

/** Toutes les pages du site, langue par langue. */
function pagesFor(lang: Lang): { ref: PageRef; page: Page }[] {
  const langPosts = byLang(posts, lang);
  return [
    { ref: { kind: "home", lang }, page: { kind: "home", posts } },
    { ref: { kind: "cv", lang }, page: { kind: "cv" } },
    { ref: { kind: "blogIndex", lang }, page: { kind: "blogIndex", posts } },
    ...langPosts.map((meta) => {
      const full = posts.find((p) => p.slug === meta.slug && p.lang === lang)!;
      return {
        ref: { kind: "post" as const, lang, slug: meta.slug },
        page: { kind: "post" as const, post: meta, html: full.html, posts },
      };
    }),
  ];
}
```

Puis, pour chaque page, la réécriture `HTMLRewriter` existante s'applique avec
en plus une réécriture des chemins d'assets de la coquille :

```ts
// La coquille bundlée référence "./assets/…" ; une page profonde a besoin de
// "../…". On réécrit src et href plutôt que de poser un <base>, qui casserait
// les liens relatifs entre pages.
const prefix = rel(pageDepth(ref));
const fixAssets = (value: string) =>
  value.startsWith("./assets/") ? prefix + value.slice(2) : value;
```

Appliquer via `.on("script[src], link[href]", …)` sur la coquille, avant
l'injection du `<head>`.

- [x] **Step 4: Lancer le build et les tests**

Run: `bun run build && bun test scripts/build.test.ts` Expected: PASS. Le compte
final annoncé par le build doit valoir
`7 × 3 + (nombre d'articles toutes langues)`.

- [x] **Step 5: Vérifier une page au navigateur**

Run: `bun run preview` (Task 15) puis ouvrir `/fr/blog/index.html` Expected: la
page s'affiche stylée, la police est chargée (pas de tofu), et la console est
vide — un asset mal résolu s'y verrait immédiatement.

- [x] **Step 6: Commit**

```bash
deno fmt scripts/build.ts scripts/build.test.ts && deno lint
git add scripts/build.ts scripts/build.test.ts
git commit -m "feat: émettre accueil, CV et pages blog par langue avec chemins relatifs"
```

---

### Task 10: Pages-relais des anciennes URLs

**Files:**

- Create: `scripts/relay.ts`, `scripts/relay.test.ts`
- Modify: `scripts/build.ts`

**Interfaces:**

- Produces:
  ```ts
  export function relayHtml(target: string, title: string): string;
  export function relayPages(): { file: string; target: PageRef }[];
  ```

**Pourquoi** : la disposition casse `fr.html` → `fr/` et supprime `en.html`. Un
stockage objet ne sait pas répondre 301 sans passer par le CDN ; la page-relais
est le seul relais qui ne dépende d'aucune configuration d'hébergeur.

- [x] **Step 1: Écrire les tests qui échouent**

```ts
import { expect, test } from "bun:test";
import { relayHtml, relayPages } from "./relay.ts";
import { LANGS } from "../src/translations.ts";

test("une page-relais renvoie, se déclare canonique et refuse l'indexation", () => {
  const html = relayHtml("./fr/index.html", "Philippe Ribeiro");

  expect(html).toContain('<meta name="robots" content="noindex" />');
  expect(html).toContain('<link rel="canonical" href="./fr/index.html" />');
  expect(html).toContain(
    'http-equiv="refresh" content="0; url=./fr/index.html"',
  );
  expect(html).toContain('<a href="./fr/index.html">');
});

test("il y a un relais par ancienne URL de langue, plus en.html", () => {
  const files = relayPages().map((p) => p.file);

  expect(files).toContain("en.html");
  for (const lang of LANGS.filter((l) => l !== "en")) {
    expect(files).toContain(`${lang}.html`);
  }
  expect(files).not.toContain("index.html"); // la racine reste une vraie page
});

test("une cible de relais est échappée", () => {
  expect(relayHtml('./x.html?a="b"', "T")).not.toContain('?a="b"');
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test scripts/relay.test.ts` Expected: FAIL —
`Cannot find module './relay.ts'`

- [x] **Step 3: Implémenter, puis câbler dans `build.ts`**

Écrire chaque relais après les pages, avec la même boucle `Bun.write`.

- [x] **Step 4: Vérifier sur le `dist/` produit**

Run: `bun run build && bun test scripts/relay.test.ts` Expected: PASS, et
`dist/fr.html` existe et pointe vers `./fr/index.html`.

- [x] **Step 5: Commit**

```bash
deno fmt scripts/relay.ts scripts/relay.test.ts scripts/build.ts && deno lint
git add scripts/relay.ts scripts/relay.test.ts scripts/build.ts
git commit -m "feat: pages-relais pour les anciennes URLs de langue"
```

---

### Task 11: Le feed porte les articles

**Files:**

- Modify: `scripts/feed.ts`, `scripts/feed.test.ts`, `scripts/build.ts`

**Interfaces:**

- Consumes: `Post` (T5).
- Produces: `jsonFeed({ lang, t, posts, homePageUrl, feedUrl, favicon })`.

**Le commentaire d'en-tête doit être réécrit.** Il pose aujourd'hui « A CV is
not a blog, so the first question is what counts as an item ». Le site devient
un blog : les articles sont les items, les entrées du CV quittent le feed. Un
abonné veut les écrits, pas la republication d'un parcours.

- [x] **Step 1: Écrire les tests qui échouent**

```ts
test("les items sont les articles de la langue, du plus récent au plus ancien", () => {
  const feed = jsonFeed({ lang: "fr", t: translations.fr, posts /* … */ });

  expect(feed.items.map((i) => i.title)).toEqual(["Titre b", "Titre a"]);
});

test("un item porte le HTML de l'article, pas seulement son texte", () => {
  const feed = jsonFeed({/* … */});
  expect(feed.items[0]!.content_html).toContain("<p>");
});

test("date_published vient du frontmatter, pas de git", () => {
  const feed = jsonFeed({/* … */});
  expect(feed.items[0]!.date_published).toBe("2026-09-01T00:00:00Z");
});

test("aucune entrée du CV ne subsiste dans le feed", () => {
  const feed = jsonFeed({/* … */});
  expect(JSON.stringify(feed)).not.toContain("Kapia");
});

test("le feed d'une langue sans article est valide et vide", () => {
  const feed = jsonFeed({ lang: "es", /* … */ posts: [] });
  expect(feed.items).toEqual([]);
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun test scripts/feed.test.ts` Expected: FAIL — `posts` n'est pas une
option de `jsonFeed`.

- [x] **Step 3: Implémenter**

Changements : `entries()` disparaît, `entryPublished()` (la recherche
`git log
-S`) disparaît **avec son test** — la date vient désormais du
frontmatter, qui est explicite et ne dépend plus de la profondeur du clone.
`content_text` devient `content_html`. L'`id` reste l'URL absolue de l'article :
stable, unique, et irrécupérable autrement.

- [x] **Step 4: Lancer les tests et vérifier qu'ils passent**

Run: `bun run build && bun test scripts/feed.test.ts` Expected: PASS, et
`dist/feed.fr.json` liste les articles français.

- [x] **Step 5: Valider le feed contre le format**

Run:
`bun -e 'const f = await Bun.file("dist/feed.json").json(); if (f.version !== "https://jsonfeed.org/version/1.1") throw new Error("version"); for (const i of f.items) if (!i.id || !i.title) throw new Error(i.id ?? "item sans id"); console.log(f.items.length, "items valides")'`
Expected: le compte des articles anglais, sans erreur.

- [x] **Step 6: Commit**

```bash
deno fmt scripts/feed.ts scripts/feed.test.ts scripts/build.ts && deno lint
git add scripts/feed.ts scripts/feed.test.ts scripts/build.ts
git commit -m "feat: le JSON Feed porte les articles au lieu des entrées du CV"
```

---

### Task 12: Le sitemap liste les articles

**Files:**

- Modify: `scripts/sitemap.ts`, `scripts/build.ts`
- Test: `scripts/sitemap.test.ts`, `scripts/build.test.ts`

- [x] **Step 1: Écrire les tests qui échouent**

```ts
test("le sitemap liste chaque page réelle, et aucun relais", async () => {
  const xml = await Bun.file("dist/sitemap.xml").text();
  const entries = await parseSitemap(xml);
  const locs = entries.map((e) => e.loc);

  expect(locs).toContain(`${BASE}/`);
  expect(locs).toContain(`${BASE}/cv.html`);
  expect(locs).toContain(`${BASE}/fr/blog/index.html`);
  expect(locs.some((l) => l.endsWith("/fr.html"))).toBe(false);
  expect(locs.some((l) => l.endsWith("/en.html"))).toBe(false);
});

test("un article est daté par son propre fichier source", async () => {
  const entries = await parseSitemap(await Bun.file("dist/sitemap.xml").text());
  const post = entries.find((e) =>
    e.loc.includes("/blog/") && !e.loc.endsWith("index.html")
  );

  expect(post?.lastmod).toMatch(/^\d{4}-\d{2}-\d{2}T/);
});
```

- [x] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `bun run build && bun test scripts/build.test.ts` Expected: FAIL — les URLs
d'articles sont absentes.

- [x] **Step 3: Implémenter**

Ajouter `postLastmod(sourcePath)` à `scripts/sitemap.ts`, sur le modèle de
`contentLastmod` :

```ts
/**
 * Quand l'article a été touché pour la dernière fois, depuis git. Un clone
 * superficiel ne sait pas répondre : on omet <lastmod> plutôt que d'inventer
 * une date, exactement comme contentLastmod.
 */
export async function postLastmod(sourcePath: string): Promise<string | null> {
  const git = await $`git log -1 --format=%cI -- ${sourcePath}`.nothrow()
    .quiet();
  const date = git.exitCode === 0 ? git.stdout.toString().trim() : "";
  return date || null;
}
```

- [x] **Step 4: Lancer les tests et vérifier qu'ils passent**

Run: `bun run build && bun test` Expected: PASS.

- [x] **Step 5: Commit**

```bash
deno fmt scripts/sitemap.ts scripts/build.ts scripts/build.test.ts && deno lint
git add scripts/sitemap.ts scripts/build.ts scripts/build.test.ts
git commit -m "feat: sitemap des pages blog, daté par fichier source"
```

---

### Task 13: Les polices couvrent le contenu

**Files:**

- Modify: `scripts/glyphs.ts`
- Test: `scripts/fonts-coverage.test.ts`

**Pourquoi** : un glyphe absent du sous-ensemble s'affiche en tofu. Le CV avait
un texte fixe ; un blog en ajoute à chaque publication.

- [x] **Step 1: Écrire le test qui échoue**

```ts
test("chaque caractère des articles est couvert par un unicode-range", async () => {
  const { latin, sc, tc, hk } = await glyphSets();
  const posts = await loadPosts();

  for (const post of posts) {
    const set = post.lang === "zh"
      ? sc
      : post.lang === "zh-hant"
      ? tc
      : post.lang === "zh-hk"
      ? hk
      : latin;

    for (const ch of post.text) {
      if (/\s/.test(ch)) continue;
      expect(set.includes(ch), `${post.sourcePath} : « ${ch} » non couvert`)
        .toBe(true);
    }
  }
});
```

- [x] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `bun test scripts/fonts-coverage.test.ts` Expected: FAIL — les caractères
du corpus ne sont pas dans les sous-ensembles.

- [x] **Step 3: Étendre `glyphSets()`**

Le texte des articles s'ajoute à la source scannée, par langue :

```ts
// Le texte brut des articles, via Bun.markdown.render — la syntaxe Markdown
// (#, *, |) ne se dessine pas, seul le texte rendu atteint la page.
const posts = await loadPosts();
const textOf = (langs: readonly Lang[]) =>
  posts.filter((p) => langs.includes(p.lang)).map((p) => `${p.title}${p.text}`)
    .join("");
```

Puis fusionner dans chaque ensemble : `latin` reçoit
`textOf(["en","fr","pt","es"])`, `sc` reçoit `textOf(["zh"])`, `tc`
`textOf(["zh-hant"])`, `hk` `textOf(["zh-hk"])`.

- [x] **Step 4: Régénérer et mesurer**

```bash
bun run fonts:update
ls -l src/fonts/*.woff2
```

Expected: PASS, et **noter les tailles avant/après dans le message de commit** —
c'est le risque nº 3 du spec, et le rendre visible est tout ce que ce plan
prétend en faire.

- [x] **Step 5: Commit**

```bash
deno fmt scripts/glyphs.ts scripts/fonts-coverage.test.ts && deno lint
git add scripts/glyphs.ts scripts/fonts-coverage.test.ts src/fonts/
git commit -m "feat: sous-ensembler les polices sur le texte des articles"
```

---

### Task 14: Le corpus de test

**Files:**

- Create: `content/posts/**` (12 articles)

**Périmètre** : 10 articles dans les **7 langues** (`zh-hk` dérivé, donc 6
fichiers chacun) + 2 partiels pour exercer le repli du sélecteur. Soit **62
fichiers** : 10 × 6 + 1 × 2 (`fr`, `en`) + 1 × 1 (`zh`).

> **Avertissement.** Ces articles sont **fictifs** et signent un site portant le
> nom réel de Philippe. Ils sont un corpus de test, pas du contenu publiable :
> le premier déploiement en production doit les remplacer ou les retirer. Le
> Plan C posera cette barrière explicitement.

- [x] **Step 1: Créer les 10 articles complets**

Sujets, choisis pour couvrir la matière que le rendu doit encaisser :

| Slug                      | Sujet                   | Ce qu'il exerce               |
| ------------------------- | ----------------------- | ----------------------------- |
| `mesurer-le-texte`        | mesure sans reflow      | `<code>` inline, lien externe |
| `sept-langues-un-site`    | le multilingue du site  | runs CJK dans du latin        |
| `knuth-plass-en-pratique` | césure et justification | paragraphes longs             |
| `bun-comme-ssg`           | le build                | bloc de code fencé long       |
| `polices-sous-ensemblees` | sous-ensemblage woff2   | table GFM                     |
| `chengdu-2011`            | récit                   | prose pure, aucun balisage    |
| `lire-en-portugais`       | lecture                 | citations, `<em>` dense       |
| `ultramarathon`           | course                  | listes à puces                |
| `le-cv-ne-postule-pas`    | pourquoi ce site        | titres `h2`/`h3` imbriqués    |
| `dix-ans-de-mesure`       | rétrospective           | le plus long, ~800 mots       |

Contraintes par fichier : frontmatter complet (`title`, `date`, `summary`,
`tags`), **300–500 mots** (sauf le dernier), dates étalées sur 2025–2026 pour
que le tri soit visible. Chaque traduction est **écrite pour son audience**, pas
convertie depuis l'anglais — c'est la règle du fichier de traductions, et elle
vaut pour le corpus.

Modèle, à suivre exactement :

```markdown
---
title: Mesurer le texte sans reflow
date: 2026-08-08
summary: Ce que pretext calcule, et ce qu'il ne calcule pas.
tags: [typographie, bun]
---

Un navigateur qui mesure du texte paie un reflow. `getBoundingClientRect` oblige
le moteur à résoudre la mise en page avant de répondre, et sur une page qui en
appelle quelques centaines, la note se voit.

## Ce que la mesure hors DOM change

…
```

- [x] **Step 2: Créer les 2 articles partiels**

- `content/posts/note-en-francais/fr.md` + `en.md` — cinq langues manquantes.
- `content/posts/shanghai-note/zh.md` — six langues manquantes.

Ils existent pour une seule raison : sans eux, le repli du sélecteur (Task 8) et
l'index d'une langue plus court que les autres ne sont jamais exercés par le
build réel.

- [x] **Step 3: Construire et vérifier le compte**

Run: `bun run build` Expected: `7 × 3 + 62 + 10` pages… soit 21 pages fixes plus
une page par `(article, langue)` existante. Le compte annoncé doit correspondre
exactement à ce que
`bun -e 'console.log((await (await import("./scripts/content.ts")).loadPosts()).length)'`
retourne, plus 21.

- [x] **Step 4: Vérifier le repli du sélecteur pour de vrai**

```bash
grep -c "n'est pas disponible dans cette langue" dist/fr/blog/note-en-francais.html
grep -c "暫無此語言版本" dist/zh-hant/blog/shanghai-note.html || true
```

Expected: la première commande trouve cinq mentions ; la seconde échoue, puisque
`shanghai-note` n'existe pas en `zh-hant` — c'est le comportement voulu,
l'article n'a pas de page dans cette langue.

- [x] **Step 5: Régénérer les polices et mesurer la croissance**

```bash
bun run fonts:update && ls -l src/fonts/*.woff2 && bun test
```

Expected: PASS. **Consigner les tailles dans le message de commit** : c'est la
première mesure réelle du risque nº 3.

- [x] **Step 6: Commit**

```bash
deno fmt content/ && deno lint
git add content/ src/fonts/
git commit -m "test: corpus de 12 articles fictifs, dont 2 partiellement traduits"
```

---

### Task 15: Prévisualiser le `dist/`

**Files:**

- Create: `scripts/preview.ts`
- Modify: `package.json`

**Interfaces:**

- Produces: `bun run preview` → serveur sur `http://localhost:4173`.

**Pourquoi** : `bun run dev` sert la coquille et garde le HMR, mais ne connaît
pas les URLs du blog. Voir un article à sa vraie URL passe par ici.

- [x] **Step 1: Implémenter**

```ts
/*
 * Serveur statique sur dist/ — la prévisualisation fidèle à la production.
 *
 * `bun run dev` reste la boucle rapide (HMR, coquille unique) ; celui-ci sert
 * l'arborescence réelle, avec les vraies URLs multilingues, et c'est le seul
 * endroit où l'on vérifie l'hydratation et les chemins d'assets relatifs.
 */

const PORT = 4173;

async function file(path: string): Promise<Response | null> {
  const candidate = Bun.file(`dist${path}`);
  return (await candidate.exists()) ? new Response(candidate) : null;
}

const server = Bun.serve({
  port: PORT,
  async fetch(req) {
    const { pathname } = new URL(req.url);
    // Un stockage objet ne résout pas les index de répertoire ; on imite le
    // minimum utile pour la relecture locale, rien de plus.
    const tries = pathname.endsWith("/")
      ? [`${pathname}index.html`]
      : [pathname, `${pathname}.html`, `${pathname}/index.html`];

    for (const path of tries) {
      const hit = await file(path);
      if (hit) return hit;
    }

    const notFound = await file("/404.html");
    return notFound
      ? new Response(notFound.body, { status: 404, headers: notFound.headers })
      : new Response("404", { status: 404 });
  },
});

console.log(`  dist/ servi sur http://localhost:${server.port}`);
```

- [x] **Step 2: Câbler le script**

```json
"preview": "bun run build && bun scripts/preview.ts"
```

- [x] **Step 3: Vérifier à la main**

```bash
bun run preview
```

Puis ouvrir `/`, `/cv.html`, `/fr/`, `/fr/blog/index.html`,
`/fr/blog/mesurer-le-texte.html` et `/inexistant`. Expected: chaque page
s'affiche stylée, police chargée, console vide ; la dernière rend le 404.

- [x] **Step 4: Commit**

```bash
deno fmt scripts/preview.ts package.json && deno lint
git add scripts/preview.ts package.json
git commit -m "feat: serveur de prévisualisation du dist/"
```

---

### Task 16: Mettre AGENTS.md en accord avec le site

**Files:**

- Modify: `AGENTS.md`, `README.md`

**Pourquoi** : `AGENTS.md` §1 décrit « le contenu vit dans src/translations.ts »
et §2 annonce « huit pages ». Les deux deviennent faux à la fin de ce plan, et
une doc fausse coûte plus cher qu'une doc absente.

- [ ] **Step 1: Corriger les affirmations devenues fausses**

- §1 « Stack and Shape » : le contenu vit désormais dans `content/posts/` pour
  les articles et `src/translations.ts` pour l'interface et le CV.
- §1 : `render.ts` est une façade sur `render/{shell,cv,blog}.ts`.
- §2 « Commands » : `bun run build` produit 21 pages fixes plus une par
  (article, langue) ; ajouter `bun run preview`.
- §2 « Generated Files » : ajouter les pages-relais.
- §9 : la négociation reste sur `index.html` seul — inchangé, mais le dire dans
  la nouvelle disposition.
- Nouvelle section : écrire un article (arborescence, frontmatter, `DRAFTS=1`,
  règle de projection `zh-hk`).

- [ ] **Step 2: Vérifier chaque commande citée**

```bash
bun run check && bun test && bun run build && bun run fonts:update
```

Expected: PASS. Toute commande citée dans `AGENTS.md` doit avoir été lancée.

- [ ] **Step 3: Commit**

```bash
deno fmt AGENTS.md README.md && deno lint
git add AGENTS.md README.md
git commit -m "docs: mettre AGENTS.md en accord avec la structure du blog"
```

---

### Task 17: Le premier article — architecture et manuel d'écriture

**Files:**

- Create: `content/posts/comment-ecrire-un-article/fr.md`
- Create: `content/posts/comment-ecrire-un-article/en.md`

**Interfaces:** aucune — c'est du contenu, produit par tout ce qui précède.

**Ce que c'est.** La documentation « comment écrire un article » **est** le
premier article du blog. Le site se documente lui-même, ce qui a une propriété
qu'aucune doc séparée n'a : si l'article s'affiche mal, la doc est fausse, et ça
se voit immédiatement.

À la différence du corpus de la Task 14, ce n'est **pas** du contenu fictif : il
a vocation à rester en production. C'est aussi le seul article que le
propriétaire n'aura pas à réécrire avant le premier déploiement.

**Deux langues, `fr` et `en`, pas les sept.** C'est délibéré et ça sert de
démonstration : l'article vitrine exerce lui-même la règle « 1..n langues » que
le site applique. Un lecteur en espagnol le verra absent de son index et
trouvera dans le sélecteur le repli vers les deux langues où il existe — le
comportement décrit dans l'article, vérifiable sur l'article lui-même.

**Articulation avec la Task 16, pour ne pas écrire deux fois la même chose :**

| Document    | Public                                             | Contenu                                         |
| ----------- | -------------------------------------------------- | ----------------------------------------------- |
| `AGENTS.md` | mainteneurs et agents                              | conventions sèches, commandes, invariants       |
| Cet article | lecteurs du site, et le propriétaire dans six mois | le récit de l'architecture, et le mode d'emploi |

Si une phrase peut aller dans les deux, elle va dans `AGENTS.md` et l'article y
renvoie. La duplication entre une doc et un article est la façon la plus sûre de
faire diverger les deux.

- [ ] **Step 1: Écrire `fr.md` — la partie architecture**

Ce qu'elle doit expliquer, dans cet ordre, en prose et non en liste à puces :

1. Pourquoi le contenu vit sur le disque en Markdown plutôt que dans
   `src/translations.ts` comme le CV.
2. Pourquoi le dossier porte le slug et le fichier porte la langue — le système
   de fichiers _est_ la donnée, donc aucune table de correspondance à tenir.
3. Ce que `Bun.markdown` apporte, et pourquoi le blog n'a aucune dépendance de
   rendu.
4. Pourquoi l'anglais est à la racine et les autres langues dans un dossier, et
   ce que les chemins relatifs achètent (un `dist/` déposable à n'importe quel
   préfixe).
5. Ce que le garde-fou garantit et ce qu'il ne garantit pas. **Sois exact** :
   c'est une liste blanche d'éléments et de schémas d'URI, pas un bac à sable.

- [ ] **Step 2: Écrire `fr.md` — le manuel**

Le mode d'emploi complet, avec les commandes réelles :

- créer `content/posts/<slug>/<lang>.md` ; les contraintes sur le slug
  (`[a-z0-9]` et des tirets, noms réservés interdits) ;
- le frontmatter, champ par champ, avec un exemple complet ;
- `draft: true` et `DRAFTS=1` pour prévisualiser sans publier ;
- `bun run preview` pour voir l'article à sa vraie URL ;
- `bun run fonts:update` après toute écriture, et **pourquoi** : un glyphe
  absent du sous-ensemble s'affiche en tofu ;
- la règle `zh-hk` : écrire `zh-hant.md` suffit, la version de Hong Kong est
  projetée par le lexique ; un `zh-hk.md` explicite l'emporte.

- [ ] **Step 3: Écrire `en.md`**

Écrit pour son lecteur, pas traduit ligne à ligne depuis le français — c'est la
règle que `src/translations.ts` applique déjà à ses sept versions.

- [ ] **Step 4: Vérifier que l'article se rend, et que le repli fonctionne**

```bash
bun run build
bun run preview
```

Ouvrir `/blog/comment-ecrire-un-article.html` et
`/fr/blog/comment-ecrire-un-article.html`. Expected: l'article s'affiche
entièrement, la police est chargée (aucun tofu), la console est vide.

Puis ouvrir `/es/blog/index.html` : l'article ne doit **pas** y figurer, et
depuis la page française, le sélecteur vers l'espagnol doit mener à l'index
espagnol avec la mention de non-disponibilité.

- [ ] **Step 5: Régénérer les polices**

```bash
bun run fonts:update && bun test
```

Expected: PASS. L'article introduit du vocabulaire technique qui n'existe pas
dans le CV (`frontmatter`, `slug`, `woff2`) — le sous-ensemble latin grandit.

- [ ] **Step 6: Vérifier que le manuel dit vrai**

Suis ton propre manuel, à la lettre, pour créer un article jetable
`content/posts/essai-du-manuel/fr.md`. Chaque commande citée doit marcher telle
quelle. Supprime l'article d'essai ensuite.

Expected: aucune étape manquante, aucune commande fausse. **Une étape qui ne
marche pas est un défaut de l'article, pas de la procédure.**

- [ ] **Step 7: Commit**

```bash
deno fmt content/ && deno lint
git add content/posts/comment-ecrire-un-article/ src/fonts/
git commit -m "docs: premier article — architecture du blog et manuel d'écriture"
```

---

## Auto-revue du plan

**Couverture du spec** — chaque section a sa tâche :

| Section du spec                     | Tâche              |
| ----------------------------------- | ------------------ |
| §4.1 arborescence, §4.3 type `Post` | T5                 |
| §4.2 frontmatter                    | T1                 |
| §4.4 pipeline Markdown              | T2                 |
| §5 disposition et profondeurs       | T3, T9             |
| §5.1 rupture d'URL, pages-relais    | T10                |
| §6.1 découpage des modules, façade  | T7                 |
| §6.2 sélecteur sur un article       | T8                 |
| §6.3–6.4 justification              | **Plan B**         |
| §6.5 chinois sans Knuth–Plass       | **Plan B**         |
| §6.6 polices                        | T13                |
| §7 build, feed, sitemap             | T9, T11, T12       |
| §8 déploiement                      | **Plan C**         |
| §9 serveur de dev                   | T15                |
| §10 tests                           | réparti sur T1–T13 |

**Écarts assumés, à valider par le relecteur :**

1. **§6.2 dit « le changement de langue sans rechargement reste sur l'accueil et
   /cv ».** Aucune tâche ne le câble : `renderPage` prend `posts` en paramètre,
   or le client ne les a pas. Il faut soit embarquer les métadonnées des cinq
   derniers articles dans le bundle, soit accepter que l'accueil se recharge
   comme un article. **Je recommande la seconde** : c'est une page, pas une
   application, et cela supprime un chemin de données entier. À trancher avant
   la Task 8.
2. **La projection `zh-hk` comble un implicite du spec** (T5). Elle mérite
   d'être remontée dans le document de design une fois validée.
3. **Le risque nº 3 (croissance CJK) est mesuré, pas traité** (T13, T14). Si les
   `.woff2` doublent, la réponse sera un sous-ensemble par page plutôt que par
   langue — hors périmètre ici.
